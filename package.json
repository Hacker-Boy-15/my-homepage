{
  "name": "my-homepage",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
  "react": "^18.0.0",
  "react-dom": "^18.0.0",
  // Other dependencies
},
  "engines": {
  "node": ">=14.0.0",
  "npm": ">=6.0.0"
},
  "scripts": {
  "build": "react-scripts build"
    "build": "next build"

},
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
